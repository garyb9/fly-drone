# Handoff: encoder v5 (plan 05), 2026-09-15

You are taking over, in /home/gb/projects/fly-drone, the execution of
`docs/superpowers/plans/2026-09-14-fly-drone-05-encoder-v5-sac.md` (spec
`docs/superpowers/specs/2026-09-14-learned-encoder-sac-design.md`). Work on `main`, commit and push after each
completed step. Read `AGENTS.md` first (rules: never relax `roam_eval.ACCEPTANCE`; always `env -u PYTHONPATH`;
≤ 6 workers; ask the user before long runs; never run `yarn py:build`/prep/ci while a simulation runs; launch
long runs with `setsid nohup`).

Commit trailers:

```
Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_013ofb1RRE8sLwD6YoXkq2Jy
```

The workspace is this folder (`.superpowers/sdd/2026-09-14-fly-drone-05-encoder-v5-sac/`, git-ignored).
`progress.md` is the ledger. It records every task completion, ruling and user decision. Trust it and
`git log` over memory. Briefs (`task-N-brief.md`) and reports live here too. Use
superpowers:subagent-driven-development for any further code work: implementer, then task review, then fixes.
Give every subagent an explicit model.

## Done

- **Code Tasks 1–10: complete and reviewed** (commits 8da2f70 … 4426754). They cover:
  - Rust `LEARNED_ENCODER_PREFIX` and the tanh output;
  - `FrameStack`, `V5_CHANNELS`, `BrainRuntime(encoder=…)`;
  - `encoder.py` (EyeNet CNN, 8 currents in [0, 2], version `learned-v5:<16 hex of sha256>`);
  - v5 sensing in `env.py`;
  - `sac.py` (SB3 2.9 SAC, `AsymmetricSACPolicy`, visible-only critic);
  - export and warm start;
  - E1/E2/E3 checks in `roam_eval`;
  - `train_round`, `validate`, `MetabolicLogger` and the CLI;
  - the docs.
- **Final whole-branch review (opus): "With fixes".**
  - 541c13a: `--seed` is now honoured on `.zip` resume; new `sac-export` command (checkpoint → encoder/decoder, re-pins a frozen decoder to a new encoder).
  - 8e0dfa8: minors (separate loom/light metabolic logging, export after the workers close, warnings for an ignored `--encoder`, markdown fix).
  - Scoped re-review (`final-rereview.md`): **Approved**. One Minor is parked: re-pinning without `--encoder` gives an opaque `torch.load(None)` traceback.
- **User decision: critic-only warm-up.** The actor and the entropy coefficient are frozen for the first
  `ACTOR_WARMUP_FRAMES = 50_000` frames of each SAC round. Implemented in **8dda027** (`WarmupSAC`, `train_round(actor_warmup=…)`,
  `--actor-warmup`, spec §4/§8 amended; 121 tests pass). Implementer concerns, from `warmup-report.md`:
  - a crash resume re-applies the warm-up (docs say to pass `--actor-warmup 0`);
  - plain-SAC zips loaded without the argument default to 50k;
  - the 6-worker path is not exercised.
  - **Its task review did NOT finish**: the reviewer died on the session limit. **Re-dispatch it first**:
    brief `warmup-brief.md`, report `warmup-report.md`, diff `review-8e0dfa8..8dda027.diff`, output `warmup-review.md`.
    Check that the actor really stays frozen: Adam state, a shared features extractor, polyak updates, and tests with real gradient steps.
- **Task 11 (Stage 1 DAgger on L2 with v4): runs FINISHED** at 2026-09-14 22:41:29, with no errors. The driver is
  `stage1-dagger.sh` here and the log is `runs/v5/dagger/stage1.log`. Screen results (L2, seeds 9000–9009, 60 s, intact):

  | policy         | beacons/min | collisions/min | visited cells |
  | -------------- | ----------- | -------------- | ------------- |
  | teacher        | 2.3         | 0.0            | 41.0          |
  | random         | 0.0         | 0.1            | 7.8           |
  | it0 warm-actor | **2.2**     | 1.0            | 35.0          |
  | it1            | 1.5         | 2.1            | 29.8          |
  | it2            | 2.0         | 0.9            | 33.6          |
  | it3            | 2.0         | 1.0            | 29.4          |

  By the rule (highest beacons/min, ties broken by fewer collisions/min) the **choice is it0**.

  **Still to do for Task 11:**
  - write `runs/v5/dagger/choice.json`:
    `{"iteration": 0, "actor": "runs/v5/dagger/it0/warm-actor.json", "beacons_per_min": 2.2, "collisions_per_min": 1.0, "teacher_beacons_per_min": 2.3}`;
  - add `Task 11: complete` to the ledger;
  - commit and push whatever the repo tracks from this (check `.gitignore` for `runs/`);
  - report to the user.

  Observation for the user: the it0 fit had held-out R² 0.38 (avoid), 0.25 (beacon) and −1.26 (explore). DAgger
  did not improve on it0 on this screen.

## Next (the user already approved "Run 11, then 12")

**Gate before Task 12:** the warm-up review above must approve (fix if not). Then run Task 12 per
`task-12-brief.md` without asking. Its step 1 "ask the user" is already satisfied. Launch long commands with
`setsid nohup … > log 2>&1 &` and watch them with a Monitor.

1. `encoder-collect --output runs/v5/clone/data.npz --flights 32 --seconds 60 --workers 6 --seed-base 600`
2. `encoder-clone runs/v5/clone/data.npz --output runs/v5/clone --steps 20000`. Report the held-out mse and r
   per channel from `clone.json`. Parked Task 4 minor: `collect_clone` reports the requested flights rather
   than the actual count, and a `_clone_job` collision only clears the frame stack. Check the counts.
3. `sac-init-decoder runs/v5/dagger/it*.npz --encoder runs/v5/clone/encoder.pt --output runs/v5/round0`.
   Expect `export_max_error ≤ 1e-4`.
4. Sanity gate: `roam-screen runs/v5/round0/decoder.json --encoder runs/v5/clone/encoder.pt --level 2 --seeds 10
--seed-base 9000 --workers 6 --output runs/v5/round0/l2-screen.json`. **If beacons/min < 0.8 × 2.2 = 1.76,
   STOP and report to the user.**
5. `sac-validate --decoder runs/v5/round0/decoder.json --encoder runs/v5/clone/encoder.pt --output runs/v5/round0/validation.json`
6. `encoder-checks --policy runs/v5/dagger/it0/warm-actor.json --output runs/v5/e1-v4-baseline.json` (v4 E1 baseline).

All of these commands are `env -u PYTHONPATH .venv/bin/fly-drone <subcommand> …`.

## After Task 12 (each needs separate user approval)

- **Before Task 13:**
  - run a short 6-worker `train_round` smoke test. Check RAM (replay buffer 100k ≈ 5.3 GB, about 0.95 GB per worker, about 18 GB free), EGL spawn, metabolic logging, that the encoder hash equals `round.json`, export parity, that the band grey varies, and that the warm-up freezes the actor on the real path;
  - tell the user the estimate: SAC rounds 1–3 take about 5–8 h, the bypass about 2 h;
  - ask for approval.
- **Task 13:** rounds 1–3 of alternating encoder/decoder SAC. **Task 14:** E3 brain bypass. **Task 15:**
  Stage 4 evaluation and results.
- **At the end:** delete this workspace, then use superpowers:finishing-a-development-branch. Report every ruling in
  `progress.md` to the user.

Fixed decisions:

- λ_loom = 0.01, λ_light = 0.002; E1 AUC ≥ 0.8; E2 margins ≥ 0.05 (light) and ≥ 0.1 (loom); E3 reported with no bar.
- Seeds: DAgger 200 + 1000k, clone 600–631, evaluation 1000–1049, validation 9000–9009.
