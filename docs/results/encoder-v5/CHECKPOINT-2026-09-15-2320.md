# Encoder v5 checkpoint (2026-09-15, 23:20 IDT)

Start here if you are resuming plan 05. It records where the run stands, what the user still has to decide, and exactly what to do next.

- Plan: `docs/superpowers/plans/2026-09-14-fly-drone-05-encoder-v5-sac.md`
- Spec: `docs/superpowers/specs/2026-09-14-learned-encoder-sac-design.md`
- Decisions log: `docs/results/2026-09-15-encoder-v5-run-decisions.md`. The latest section is "Round 1: result and where we stand".
- Ledger:
  - live: `.superpowers/sdd/2026-09-14-fly-drone-05-encoder-v5-sac/progress.md`
  - committed copy: `docs/results/encoder-v5/ledger.md`
- Earlier handoff: `docs/results/encoder-v5/handoff-2026-09-15.md`

## Status by task

| Task                                    | State                                                                                                                               |
| --------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 1–11                                    | Complete and reviewed (see ledger)                                                                                                  |
| 12 (clone, DAgger round 0, sanity gate) | Complete. Round 0 scored 2.13±0.27 beacons/min on 30 seeds, against 1.93 for v4, so the gate passed                                 |
| Smoke test before Task 13               | Passed (`runs/v5/diag/smoke_check.py`, SMOKE_CHECK OK)                                                                              |
| 13 (SAC rounds 1–3)                     | **Running.** Round 1 is done but degenerate. Round 2 encoder SAC was at 315k/350k frames at 23:20, ep_rew ≈ −2500                   |
| 14 (bypass / E3)                        | Not started. The pipeline runs it automatically after Task 13                                                                       |
| 15 (Stage 4 eval, results)              | Not started. The pipeline runs the evaluation, but the results write-up, validation.md and the A/E report are manual                |
| Final review, delete workspace, finish branch | Not started                                                                                                                   |

## Validation of the rounds so far

| Round | near_dodge | balanced | ghost | beacons/min | collisions/min | E1 AUC        | E2                                     |
| ----- | ---------- | -------- | ----- | ----------- | -------------- | ------------- | -------------------------------------- |
| 0     | 0.269      | 0.182    | 0.296 | 1.9         | 2.7            | 0.686         | pass (0.50 / 0.26)                     |
| 1     | 0.926      | 0.909    | 0.815 | **0.0**     | 6.1            | 0.480 (fail)  | fail (light −0.041, loom −0.047)       |

Round 1 is degenerate. Erratic flight counts as dodging even with the eyes blind: ghost is 0.815 against a bar of ≤ 0.3. The drone collects no beacons, and loom and light carry no information (E1/E2 at chance). That is not causal behaviour.

**Pipeline defect:** both the stop rule and Step 6 in `pipeline13-15.sh` select on near_dodge_rate alone, with ties broken by beacons. They accepted round 1 as the best so far, and Step 6 will copy whichever round has the highest near_dodge to `runs/v5/final/`. **Expect a degenerate pair there.**

## Live processes

- The pipeline is `bash .superpowers/sdd/.../pipeline13-15.sh`, pid **788490** (process-group leader), started about 18:58, log `runs/v5/pipeline13-15.log`.
  - Current step: round 2 encoder SAC, log `runs/v5/round2-encoder.log`.
  - Next steps, in order:
    1. Round 2 decoder SAC (150k frames)
    2. sac-validate (≈ 23:40–00:15)
    3. Stop rule: round 3, or Step 6 final pick
    4. Bypass (450k frames), then E3 and the Stage 4 evaluation
- Stop it with `pkill -TERM -g $(ps -o pgid= -p 788490)`. **The user must run or approve this**, because the auto-mode classifier denied the agent's pause attempt.
- Nothing in the pipeline touches `accepted-policies.json` or `roam_eval.ACCEPTANCE`.
- Resources at 23:20: project 12 G including .venv (6.3 G without), 9 G RAM available. The limits are 20 G of disk and 3 G of free RAM.

## Pending user decision

Stop the pipeline now, or let round 2 finish.

- **Agent recommendation:** stop, and keep round 0 as the best valid pair (`runs/v5/round0/decoder.json` with `runs/v5/clone/encoder.pt`).
- Before re-running rounds:
  - Add a selection guard to the stop rule and Step 6: beacons/min ≥ ½ of round 0 and ghost dodge ≤ 0.3.
  - Investigate why decoder SAC collapsed. Reward sat flat around −690 after the actor unfroze while ent_coef fell from 1 to 0.004. Suspects are an entropy/α problem after the unfreeze and reward shaping that makes erratic flight pay.
- ACCEPTANCE thresholds stay untouched.
- The user said jerky flight is fine ("isn't that how a fly flies?"). The answer given: jerkiness is fine; non-causal dodging and zero foraging are not.

## Resume: what to do next

1. `ps -p 788490` and `tail -n 30 runs/v5/pipeline13-15.log` show how far it got.
2. Check `runs/v5/round2/validation.json` and whether `runs/v5/final/choice.json` exists. If it does, check which round it picked. If it picked round 1 or 2 and that round has ghost > 0.3 or beacons ≈ 0, **the Task 14/15 numbers are about a degenerate actor. Report them as such and don't accept it.**
3. Record every new result in:
   - the ledger (then re-copy it to `docs/results/encoder-v5/ledger.md`)
   - the decisions doc
   - the run records under `docs/results/encoder-v5/run-records/roundN/`
   Commit by explicit path (never `git add -A`; `docs/superpowers/specs/2026-09-15-training-improvements.md` belongs to another session) and push.
4. If the user approves stopping and fixing:
   - Implement the selection guard.
   - Diagnose the SAC collapse, using SDD with explicit models.
   - Re-plan Tasks 13–15 with the user before any new long run.
5. Task 15: report **every** A1–A6 and E1–E4 line. Don't propose threshold changes. Add to accepted-policies.json only if A1–A6, E1, E2 and E4 pass **and** the user agrees.
6. Cleanup (roughly 4 GB removable): only after Task 15, and only with the user's go-ahead.

## Rules (carry forward)

- Always `env -u PYTHONPATH`.
- ≤ 6 workers.
- Launch long runs with `setsid nohup`.
- No `yarn py:build`/prep/ci while a simulation runs.
- Commit and push after each step.
- The legacy room MJCF and accepted actors stay reproducible.
- Ask the user before new long data-collection or training runs.
