# S0 — managed experiments and evidence completeness

The approved roadmap is committed. The shipped codec remains v3 and all canonical brain/actor
artifacts and acceptance thresholds are unchanged. S0 adds execution/evidence infrastructure,
not a behavioral improvement.

## Delivered

- Foreground `experiment start/status/stop/resume`, repository-wide serialization and budget ledger.
- Atomic status, progress events, owned-worker cancellation, preserved partial/completed outputs,
  manifest/source/artifact checks, cumulative ≤2h/run and ≤6h/stage. Worker counts are a declared
  child-program contract, not an operating-system resource container.
- Complete diagnostic execution is not a scientific pass. Missing preregistered outcome criteria
  produce `incomplete` pending interpretation, even when all trials finish successfully.
- New evaluation reports have independent behavior/provenance/realtime completeness. No missing
  A5 or ghost trials, or absent A7 measurements, may support promotion. Current-pointer selection
  honors this field; old report semantics and acceptance arithmetic remain compatible.
- Sibling source inventory fingerprints the dirty body work without changing it. It identifies
  authored controllers/forces and explicitly does not certify neural-only behavior.

## Validation

`env -u PYTHONPATH MUJOCO_GL=egl .venv/bin/python -m pytest -q tests/test_experiments.py
tests/test_a1_audit.py tests/test_report_completeness.py tests/test_current_pointer.py
tests/test_cli.py`: **71 passed**. Ruff and whitespace checks pass for the new/changed files.

The first whole-suite invocation without explicit headless rendering aborted inside MuJoCo
renderer creation. An EGL-configured full-suite run is ongoing at this checkpoint; no full-suite
success is claimed here. The baseline provenance JSON records the source/artifact snapshot.

## Next

The registered S1 pilot runs development seed 0 in synthetic and rendered batteries; development
seeds 1–7 and confirmation 8–15 follow only after its execution is validated. Manifests are adjacent.
The immutable battery compares identical neural traces across codecs and records mechanical yaw;
it is an open-loop diagnostic, not A5 seeking acceptance. No bridge/default change is authorized
by a correct sign alone. After the bounded audit, continue body work per user priority.
