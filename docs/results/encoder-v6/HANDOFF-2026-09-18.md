# Encoder v6 — post-run handoff and decision brief (2026-09-18)

**Status:** v6 is **not finished**. Three encoder-training routes have clean negative results; the best
valid pair is `runs/v6/round0`. One user decision blocks the next implementation plan. This brief is
the handoff; there is **no plan 08 and no post-v6 spec yet** — both depend on that decision.

Companions: [`RESULTS-2026-09-16.md`](RESULTS-2026-09-16.md), [`JOINT-2026-09-17.md`](JOINT-2026-09-17.md),
the v6 design [`2026-09-16-retinotopic-sensing-v6-design.md`](../../superpowers/specs/2026-09-16-retinotopic-sensing-v6-design.md) §5–§8,
and plan 07 [`2026-09-17-fly-drone-07-gated-joint-training.md`](../../superpowers/plans/2026-09-17-fly-drone-07-gated-joint-training.md).

## 1. Where v6 stands

| Route | Run | Outcome |
| --- | --- | --- |
| Neutral clone + round-0 decoder | `runs/v6/round0` | Foraging gate **pass** (2.00 beacons/min, v4 ref 1.933); E1 loom **0.720** (v4 0.732), E2 pass. **Valid but not selective.** |
| `seed_motion` re-clone | — | Gate **fail** (1.50 vs 1.547): driving the loom circuit from a global scalar hurts foraging. |
| Alternating encoder round | `runs/v6/round1` | **Reject**: beacons 0.00, ghost 0.875, E1 loom 0.526, E2 fail. Predictive + strong anchor did not hold. |
| Joint training, single α (M4) | `runs/v6/joint/round1` | **Reject**: beacons 0.00, ghost 1.00, E1 union 0.492 (chance), E2 fail. Auto-α drifted 9.8e-5 → 0.048. |
| Joint training, per-head α | `runs/v6/joint/round2` | **Collapse at 100k** (liveness 0.000/0.000), stopped ~160k; velocity α crashed to 3.6e-7. |

The liveness gate (`scripts/encoder_liveness.py`) never caught the failures: the representation stayed
non-constant while losing loom/motion selectivity. **Variance is necessary, not sufficient; E1 is the
gate that catches semantic collapse.**

## 2. What "v6 finished" would mean

A pair that (a) passes `roam_eval.round_eligible`, (b) beats round 0 on the E1 **union** with E2
passing, and (c) fails E3 (a bypass decoder reading the 816 currents is **not** better) — i.e. the
M4 acceptance from the v6 spec §6/§8. Note the pre-registered E1 bar (0.8) is above **both** v4
(0.732) and round 0 (0.720), so reaching it is a separate, harder question than beating round 0.

Alternatively, "finished" may mean an explicit decision to stop tuning the encoder and ship the
round-0 pair into decoder training.

## 3. The blocking decision (JOINT §6)

**Decided 2026-09-18: Option 3 — freeze the encoder.** Rationale: three routes negative; `round 0`
is a valid flying pair; the E1 bar sits above both v4 and round 0, while the goal is behaviour and
causal attribution; the survey shows every strong project learns only the readout. Option 2 stays a
time-boxed fallback if A3 (threat dodge) fails specifically on loom. See
[plan 08](../../superpowers/plans/2026-09-18-fly-drone-08-frozen-v6-free-roam.md).

- **Option 2 — decouple the critic.** Keep the velocity head out of the joint entropy path; train it
  against an anchored encoder, and update the encoder only through the predictive objective plus an
  E1-style anti-wire objective. This is the remaining research attempt to make the encoder *improve*
  E1.
- **Option 3 — freeze the encoder.** Accept the neutral clone and train **only the decoder**. This
  abandons "SAC improves E1" but preserves the whole v6 sensing pipeline (patch currents, spatial
  silencing, retinotopic map) and unblocks free-roam behaviour now. **Chosen.**

Tension to resolve: the project goal (AGENTS.md, v6 spec §2) permits a **learned** encoder for free
roam; Option 3 is a declared scoped exception, not a silent revert. The survey in
[`docs/external-prior-art.md`](../../external-prior-art.md) supports it: the strong external projects
treat the visual front end as fixed/engineered and put learning in the readout.

## 4. Sequence after the decision

1. **Plan 08 written** —
   [`2026-09-18-fly-drone-08-frozen-v6-free-roam.md`](../../superpowers/plans/2026-09-18-fly-drone-08-frozen-v6-free-roam.md):
   freeze `learned-v6:01280e414169ff9c`, realisability probe, E3 bypass control, DAgger 0–3,
   decoder SAC under the frozen encoder, A1–A7, then the harvest diagnostics.
2. **M5 — unseen-layout generalization** (v6 spec §8): train on a distribution of layouts, evaluate
   on a shifted one, final E1–E4.
3. **Free-roam actor** (overview §10, now on the v6/frozen pair): DAgger iterations 0–3 → PPO →
   full `roam_eval` A1–A7 → accepted actor in `docs/results/accepted-policies.json`.
4. **Use the harvest diagnostics** on the accepted pair: rewired control (B), eye-alignment report
   (A), stimulus battery (D), fly.ai oracle (C) — all landed 2026-09-18.

## 5. Ready vs missing

| Ready | Missing |
| --- | --- |
| v6 spec §5–§8; plan 06; plan 07 (draft, routes failed); RESULTS/JOINT records | **Post-v6 M5 evaluation plan** (unseen-layout + final E1–E4) |
| Harvest spec + B/A/D/C/E scripts and diagnostics | DAgger/SAC run records (plan 08 Tasks 1–4 not yet run) |
| `runs/v6/round0` valid pair; `encoder_liveness.py`; round guard; **plan 08** (frozen-v6 free roam) | — |

## 6. Next action

Execute [plan 08](../../superpowers/plans/2026-09-18-fly-drone-08-frozen-v6-free-roam.md), starting at
Task 0 (pin the frozen pair and smoke the command paths). Tasks 1–2 (realisability, E3 bypass) are
cheap and gate the long DAgger/SAC runs; ask the user before each long run.
